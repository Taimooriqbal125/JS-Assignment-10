// Write a JS program to find whether a number is negative,positive,0 ?

var number = 10;
if (number > 0){
    console.log("Number is positive")
}
else if (number < 0){
    console.log("Number is negative")
}
else if (number == 0){
    console.log("Number is zero")
}
else{
    console.log("noresult")
}

// write a js program to input any character ,and check whether it is vowals or pr consont?

var characters = 'e';

if (characters == 'a' || characters  == 'e' || characters == 'i' || characters == 'o' || characters == 'u') {
    console.log("Character is Vowal ")
}
else if ( characters != 'a' || characters != 'e' || characters != 'i' || characters != 'o' || characters != 'u')
    {
        console.log("Character  is not vowal ")
    }
    

    // Write a Js program to input any alphabet and check whether it is alphabet , digit or special character ?
    
    var char = 11 ;
    if (char <= "z"){
        console.log("Alphabet")
    }
    else if(char >= 0 || char >= 9){
        console.log("Digit")
    }    
    else{
        console.log("Special ")
    }

    // Write a JS program to check whether a character is uppercase or lowercase alphabet?

    var alph = "A";
    if(alph <= "Z"){
        console.log("Upper case")
    }
    else if(alph <= "z"){
        console.log("LowerCase")
    }
      // Write a JS program to check whether a character is uppercase or lowercase alphabet?

      var alph = "A";
      if(alph <= "Z"){
          console.log("Upper case")
      }
      else if(alph <= "z"){
          console.log("LowerCase")
      }
  
